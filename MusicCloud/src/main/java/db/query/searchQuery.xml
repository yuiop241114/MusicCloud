<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<entry key="searchList">
	
SELECT 
       MUSIC_NO,
       MUSIC_TITLE,
       MUSIC_SINGER,
       MUSIC_IMAGE_EDIT_NAME,
       MUSIC_IMAGE_PATH
FROM MUSIC_LIST
JOIN MUSIC_FILE 
     USING (MUSIC_NO)
WHERE MUSIC_TITLE LIKE ?
ORDER BY 
    REGEXP_COUNT(MUSIC_TITLE, ?) DESC,  		
    LENGTH(MUSIC_TITLE) ASC,       				        
    INSTR(MUSIC_TITLE, ?) ASC 			
	</entry>
	
	<entry key="searchListPopular">
	
	SELECT MUSIC_NO
	     , MUSIC_TITLE
	     , MUSIC_SINGER
	     , MUSIC_IMAGE_EDIT_NAME
	     , MUSIC_IMAGE_PATH
	     , (MUSIC_COUNT + MUSIC_LIKE * 5 - MUSIC_DISLIKE) AS "POPULAR"
	 FROM MUSIC_LIST
	 JOIN MUSIC_FILE USING (MUSIC_NO)
	WHERE MUSIC_TITLE 
	      LIKE ?
	ORDER BY POPULAR DESC
	</entry>
</properties>